<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Video Chat Room</title>

  <script>const ROOM_ID = "<%= roomId %>";</script>
  <script defer src="https://unpkg.com/peerjs@1.2.0/dist/peerjs.min.js"></script>
  <script defer src="https://cdn.socket.io/4.8.1/socket.io.min.js"></script>
  <script defer src="/script.js"></script>

  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    body {
      background: radial-gradient(circle at center, #e3f9a6, #b2f2c3, #c4f0ed);
      color: #1f1f1f;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      padding-top: 5rem;
      padding-bottom: 8rem;
    }

    /* Header / Navbar */
    nav {
      width: 100%;
      background-color: transparent;
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 1.2rem 2rem;
      position: fixed;
      top: 0;
      left: 0;
      z-index: 10;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.05);
    }

    .logo {
      font-weight: bold;
      font-size: 1.5rem;
      color: #1b4332;
    }

    .nav-links a {
      margin-left: 20px;
      text-decoration: none;
      color: #333;
      font-weight: 500;
    }

    .login-btn {
      padding: 8px 18px;
      border: 1px solid #1b4332;
      background: none;
      color: #1b4332;
      border-radius: 5px;
      cursor: pointer;
      font-weight: bold;
    }

    h1 {
      margin-bottom: 1rem;
      font-size: 2rem;
      color: #1b4332;
      margin-top: 1rem;
    }

    #video-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
      gap: 20px;
      width: 100%;
      max-width: 1200px;
      padding: 1rem;
    }

    video {
      width: 100%;
      height: 100%;
      object-fit: cover;
      background: #000;
      border-radius: 16px;
      box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1);
      border: 3px solid #c9f4e4;
    }

    #chat-container {
      position: fixed;
      bottom: 0;
      left: 0;
      width: 100%;
      background: #fff;
      border-top: 1px solid #ccc;
      padding: 10px 20px;
      box-shadow: 0 -2px 5px rgba(0, 0, 0, 0.1);
    }

    #messages {
      list-style: none;
      margin: 0;
      padding: 0;
      max-height: 150px;
      overflow-y: auto;
      font-size: 14px;
    }

    #chat-form {
      display: flex;
      margin-top: 5px;
    }

    #message-input {
      flex: 1;
      padding: 8px;
      border: 1px solid #ccc;
      border-radius: 4px;
    }

    #chat-form button {
      padding: 8px 12px;
      margin-left: 5px;
      border: none;
      background: #007bff;
      color: white;
      border-radius: 4px;
      cursor: pointer;
    }
  </style>
</head>

<body>
  <!-- Header/Navbar -->
  <nav>
    <div class="logo">OpenChatU</div>
    <div class="nav-links">
      <a href="#">Home</a>
      <a href="#">Counseling</a>
      <a href="#">Schedule</a>
      <a href="#">Support</a>
      <button class="login-btn">Logout</button>
    </div>
  </nav>

  <h1>Welcome to the OpenChatU Video Room</h1>
  <div id="video-grid"></div>

  <div id="chat-container">
    <ul id="messages"></ul>
    <form id="chat-form">
      <input id="message-input" autocomplete="off" placeholder="Type a message..." />
      <button>Send</button>
    </form>
  </div>
</body>
</html>
